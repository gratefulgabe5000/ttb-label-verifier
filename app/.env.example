# Database (SQLite — CR-003)
DATABASE_URL=sqlite:///./data/workingfiles.db

# Uploaded file storage (form PDFs + label images, A-16/SR-003)
UPLOAD_DIR=./data/uploads

# JWT auth (DevLog Decision 8 #15 — python-jose + passlib)
JWT_SECRET=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# Demo agent accounts (WBS 3.1) — created by running `python seed.py` once
# the database has been initialized. Use these to log in to the frontend:
#   Username: agent1   Password: password123
#   Username: agent2   Password: password123

# CORS — comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:5173

# Optional: explicit path to the tesseract binary for pytesseract (WBS 6.0,
# FR-040). Needed on Windows dev machines, where the UB-Mannheim Tesseract
# installer does not add itself to PATH. Leave unset on Linux/Railway, where
# the apt-installed tesseract-ocr package is already on PATH.
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# NOTE: ANTHROPIC_API_KEY is intentionally NOT configured here.
# Each agent provides their own Anthropic API key at runtime via the
# frontend Settings panel (gear icon). The key is held only in this
# server process's environment for the life of the process — it is
# never written to disk, to this file, or to the database.
